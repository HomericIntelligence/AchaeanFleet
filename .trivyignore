# .trivyignore
#
# CVEs that have been reviewed and accepted as unfixable or false-positive
# for the AchaeanFleet container images.
#
# Format: each entry MUST be preceded by a 6-field structured comment block
# per SECURITY.md "CVE Suppression Policy" / "Required entry format":
#
#   # <CVE-ID>
#   # Category: OS | npm | python | other
#   # Rationale: <why it cannot be fixed here>
#   # Exploitability: <none | low | medium | ...> — justification
#   # Expires: YYYY-MM-DD  (max 90 days from approval; re-review before)
#   # Reviewer: @<github-handle>  PR #<number>
#   <CVE-ID>
#
# Reference: https://aquasecurity.github.io/trivy/latest/docs/configuration/filtering/#trivyignore

# CVE-2024-21538
# Category: npm
# Rationale: cross-spawn ReDoS — bundled node_modules of @anthropic-ai/claude-code (pinned 2.1.163; bumped to 2.1.233 in the #760 upgrade PR) — removable if the bump clears the bundled dep (confirm via next scan).
# Exploitability: low — no attacker-controlled input reaches cross-spawn in the vessel runtime context.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2024-21538

# CVE-2025-64756
# Category: npm
# Rationale: glob command injection — bundled node_modules of @anthropic-ai/claude-code (pinned 2.1.163; bumped to 2.1.233 in the #760 upgrade PR) — removable if the bump clears the bundled dep (confirm via next scan).
# Exploitability: none — glob inputs are hard-coded in the bundled package.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2025-64756

# CVE-2026-26996
# Category: npm
# Rationale: minimatch DoS — bundled node_modules of @anthropic-ai/claude-code (pinned 2.1.163; bumped to 2.1.233 in the #760 upgrade PR) — removable if the bump clears the bundled dep (confirm via next scan).
# Exploitability: low — minimatch input limited to bundled-package code paths.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2026-26996

# CVE-2026-27903
# Category: npm
# Rationale: minimatch DoS — bundled node_modules of @anthropic-ai/claude-code (pinned 2.1.163; bumped to 2.1.233 in the #760 upgrade PR) — removable if the bump clears the bundled dep (confirm via next scan).
# Exploitability: low — minimatch input limited to bundled-package code paths.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2026-27903

# CVE-2026-27904
# Category: npm
# Rationale: minimatch DoS — bundled node_modules of @anthropic-ai/claude-code (pinned 2.1.163; bumped to 2.1.233 in the #760 upgrade PR) — removable if the bump clears the bundled dep (confirm via next scan).
# Exploitability: low — minimatch input limited to bundled-package code paths.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2026-27904

# CVE-2026-23745
# Category: npm
# Rationale: node-tar arbitrary file overwrite — bundled node_modules of @anthropic-ai/claude-code (pinned 2.1.163; bumped to 2.1.233 in the #760 upgrade PR) — removable if the bump clears the bundled dep (confirm via next scan).
# Exploitability: none — no attacker-controlled tar input reaches the bundled tar.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2026-23745

# Cross-reference: .pip-audit-ignore.aider-chat.txt covers pip-audit findings for the same
# aider-chat dependency tree (57 GHSA/PYSEC IDs). Trivy scans container image layers;
# pip-audit scans Python wheel metadata. Dual suppression is intentional.
# When removing an ID, search both files. See docs/runbooks/dependency-audit-allowlists.md.
# Note: GHSA-69x8-hrgq-fjj8 below also appears in .pip-audit-ignore.aider-chat.txt.

# CVE-2026-35030
# Category: python
# Rationale: litellm@1.68.0 CRITICAL auth bypass — fixed in 1.83.0, present in the stale published achaean-aider image (aider-chat 0.82.3-era dep tree); repo now pins aider-chat 0.86.2 + security overrides (vessels/aider/requirements-security-overrides.txt); entry removable after the vessel image is rebuilt (aider vessel disabled in CI per #665)
# Exploitability: low — vessel does not expose litellm auth endpoints externally.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2026-35030

# CVE-2026-35029
# Category: python
# Rationale: litellm@1.68.0 HIGH RCE — fixed in 1.83.0, present in the stale published achaean-aider image (aider-chat 0.82.3-era dep tree); repo now pins aider-chat 0.86.2 + security overrides (vessels/aider/requirements-security-overrides.txt); entry removable after the vessel image is rebuilt (aider vessel disabled in CI per #665)
# Exploitability: low — vessel runs litellm only via aider-chat's internal call paths.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2026-35029

# GHSA-69x8-hrgq-fjj8
# Category: python
# Rationale: litellm@1.68.0 HIGH password hash exposure — fixed in 1.83.0, present in the stale published achaean-aider image (aider-chat 0.82.3-era dep tree); repo now pins aider-chat 0.86.2 + security overrides (vessels/aider/requirements-security-overrides.txt); entry removable after the vessel image is rebuilt (aider vessel disabled in CI per #665).
# Exploitability: low — vessel does not persist litellm-managed credentials.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
GHSA-69x8-hrgq-fjj8

# CVE-2025-69223
# Category: python
# Rationale: aiohttp@3.11.18 HIGH HTTP parser — fixed in 3.13.3, present in the stale published achaean-aider image (aider-chat 0.82.3-era dep tree); repo now pins aider-chat 0.86.2 + security overrides (vessels/aider/requirements-security-overrides.txt); entry removable after the vessel image is rebuilt (aider vessel disabled in CI per #665).
# Exploitability: low — aiohttp invoked only against trusted upstream model APIs.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2025-69223

# CVE-2025-48379
# Category: python
# Rationale: pillow@11.2.1 DDS heap buffer overflow — fixed in 11.3.0, present in the stale published achaean-aider image (aider-chat 0.82.3-era dep tree); repo now pins aider-chat 0.86.2 + security overrides (vessels/aider/requirements-security-overrides.txt); entry removable after the vessel image is rebuilt (aider vessel disabled in CI per #665).
# Exploitability: none — aider-chat does not load attacker-supplied DDS images.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2025-48379

# CVE-2026-25990
# Category: python
# Rationale: pillow@11.2.1 OOB write — fixed in 12.1.1, present in the stale published achaean-aider image (aider-chat 0.82.3-era dep tree); repo now pins aider-chat 0.86.2 + security overrides (vessels/aider/requirements-security-overrides.txt); entry removable after the vessel image is rebuilt (aider vessel disabled in CI per #665).
# Exploitability: none — aider-chat does not load attacker-supplied images.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2026-25990

# CVE-2026-40192
# Category: python
# Rationale: pillow@11.2.1 decompression bomb DoS — fixed in 12.2.0, present in the stale published achaean-aider image (aider-chat 0.82.3-era dep tree); repo now pins aider-chat 0.86.2 + security overrides (vessels/aider/requirements-security-overrides.txt); entry removable after the vessel image is rebuilt (aider vessel disabled in CI per #665).
# Exploitability: none — aider-chat does not load attacker-supplied images.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2026-40192

# CVE-2025-4565
# Category: python
# Rationale: protobuf@5.29.4 unbounded recursion — fixed in 5.29.5, present in the stale published achaean-aider image (aider-chat 0.82.3-era dep tree); repo now pins aider-chat 0.86.2 + security overrides (vessels/aider/requirements-security-overrides.txt); entry removable after the vessel image is rebuilt (aider vessel disabled in CI per #665).
# Exploitability: low — protobuf input limited to model-API payloads.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2025-4565

# CVE-2026-0994
# Category: python
# Rationale: protobuf@5.29.4 DoS — fixed in 5.29.6, present in the stale published achaean-aider image (aider-chat 0.82.3-era dep tree); repo now pins aider-chat 0.86.2 + security overrides (vessels/aider/requirements-security-overrides.txt); entry removable after the vessel image is rebuilt (aider vessel disabled in CI per #665).
# Exploitability: low — protobuf input limited to model-API payloads.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2026-0994

# CVE-2026-23490
# Category: python
# Rationale: pyasn1@0.6.1 memory exhaustion — fixed in 0.6.2, present in the stale published achaean-aider image (aider-chat 0.82.3-era dep tree); repo now pins aider-chat 0.86.2 + security overrides (vessels/aider/requirements-security-overrides.txt); entry removable after the vessel image is rebuilt (aider vessel disabled in CI per #665).
# Exploitability: none — pyasn1 not exposed to attacker-controlled input.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2026-23490

# CVE-2026-30922
# Category: python
# Rationale: pyasn1@0.6.1 DoS — fixed in 0.6.3, present in the stale published achaean-aider image (aider-chat 0.82.3-era dep tree); repo now pins aider-chat 0.86.2 + security overrides (vessels/aider/requirements-security-overrides.txt); entry removable after the vessel image is rebuilt (aider vessel disabled in CI per #665).
# Exploitability: none — pyasn1 not exposed to attacker-controlled input.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2026-30922

# CVE-2025-66418
# Category: python
# Rationale: urllib3@2.4.0 decompression — fixed in 2.6.0, present in the stale published achaean-aider image (aider-chat 0.82.3-era dep tree); repo now pins aider-chat 0.86.2 + security overrides (vessels/aider/requirements-security-overrides.txt); entry removable after the vessel image is rebuilt (aider vessel disabled in CI per #665).
# Exploitability: low — urllib3 invoked only against trusted upstream model APIs.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2025-66418

# CVE-2025-66471
# Category: python
# Rationale: urllib3@2.4.0 streaming — present in the stale published achaean-aider image (aider-chat 0.82.3-era dep tree); repo now pins aider-chat 0.86.2 + security overrides (vessels/aider/requirements-security-overrides.txt); entry removable after the vessel image is rebuilt (aider vessel disabled in CI per #665).
# Exploitability: low — urllib3 invoked only against trusted upstream model APIs.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2025-66471

# CVE-2026-21441
# Category: python
# Rationale: urllib3@2.4.0 decompression bomb — fixed in 2.6.3, present in the stale published achaean-aider image (aider-chat 0.82.3-era dep tree); repo now pins aider-chat 0.86.2 + security overrides (vessels/aider/requirements-security-overrides.txt); entry removable after the vessel image is rebuilt (aider vessel disabled in CI per #665).
# Exploitability: low — urllib3 invoked only against trusted upstream model APIs.
# Expires: 2026-11-13  (re-reviewed 2026-08-15: still flagged in latest scan)
# Reviewer: @mvillmow  PR #TBD (re-review 2026-08-15)
CVE-2026-21441

# CVE-2026-39823
# Category: other
# Rationale: Go stdlib URL canonicalization — embedded in /usr/local/bin/yq (mikefarah/yq v4.53.2 built with Go 1.26.2). Fixed in Go 1.25.10 / 1.26.3; no newer yq release available as of 2026-05-29 that bundles a patched Go toolchain.
# Exploitability: none — yq in the achaean-worker vessel processes only local YAML files and does not handle attacker-controlled URLs.
# Expires: 2026-08-27
# Reviewer: @mvillmow  PR #658
CVE-2026-39823

# CVE-2026-39825
# Category: other
# Rationale: Go stdlib net/http ReverseProxy query parameter forwarding — embedded in /usr/local/bin/yq (mikefarah/yq v4.53.2 built with Go 1.26.2). Fixed in Go 1.25.10 / 1.26.3; no newer yq release available as of 2026-05-29.
# Exploitability: none — yq in the achaean-worker vessel is a YAML processor; it does not act as an HTTP reverse proxy and does not forward query parameters.
# Expires: 2026-08-27
# Reviewer: @mvillmow  PR #658
CVE-2026-39825

# CVE-2026-39826
# Category: other
# Rationale: Go stdlib html/template XSS via trusted template author script tag — embedded in /usr/local/bin/yq (mikefarah/yq v4.53.2 built with Go 1.26.2). Fixed in Go 1.25.10 / 1.26.3; no newer yq release available as of 2026-05-29.
# Exploitability: none — yq in the achaean-worker vessel processes YAML, not HTML templates; the html/template code path is unreachable in this usage.
# Expires: 2026-08-27
# Reviewer: @mvillmow  PR #658
CVE-2026-39826

# CVE-2026-39822
# Category: other
# Rationale: Go stdlib os.Root symlink-following fix exists (Go 1.25.12 / 1.26.5) but no upstream binary rebuilt with a patched toolchain is available yet: latest GitHub CLI release v2.96.0 (2026-07-02, /usr/bin/gh in achaean-claude) is built with Go 1.26.4, and yq v4.53.3 (2026-06-06, /usr/local/bin/yq in achaean-worker) is also built with Go 1.26.4 — both predate the 1.26.5 fix. Tracking removal in issue #720.
# Exploitability: low — os.Root traversal requires an attacker able to pre-place symlinks in directories the binary opens via os.Root; gh and yq run as developer CLIs inside the vessel with no attacker-controlled filesystem input path in this container's runtime context.
# Expires: 2026-10-12
# Reviewer: @mvillmow  PR #721
CVE-2026-39822

# CVE-2026-14257
# Category: npm
# Rationale: brace-expansion DoS — bundled with npm 11.18.0's dependency tree in achaean-base-* images. npm's bundled module tree cannot be overridden without forking npm; the fixed versions (5.0.8+) ship in newer npm releases than the pinned 11.18.0.
# Exploitability: none — brace-expansion input is npm-internal and hard-coded, never attacker-controlled in the vessel runtime.
# Expires: 2026-11-05
# Reviewer: @mvillmow  PR #766
CVE-2026-14257

# CVE-2026-69152
# Category: npm
# Rationale: second brace-expansion DoS — same bundled-npm dependency tree as CVE-2026-14257.
# Exploitability: none — npm-internal input only.
# Expires: 2026-11-05
# Reviewer: @mvillmow  PR #766
CVE-2026-69152

# CVE-2026-69192
# Category: npm
# Rationale: ip-address inconsistent parsing CVE — bundled with npm 11.18.0's dependency tree in achaean-base-* images (fixed in ip-address 10.3.1, ships in newer npm than pinned 11.18.0). npm's bundled module tree cannot be overridden without forking npm.
# Exploitability: none — ip-address input is npm-internal and hard-coded, never attacker-controlled in the vessel runtime.
# Expires: 2026-11-05
# Reviewer: @mvillmow  PR #746
CVE-2026-69192

# CVE-2026-56852
# Category: other
# Rationale: golang.org/x/text DoS via invalid UTF-8 in the yq go binary bundled in achaean-base-* images. yq v4.53.3 (the latest release, 2026-06-06) still vendors x/text v0.37.0; fixed 0.39.0 is not yet available in any yq release, so the image cannot be upgraded to fix it.
# Exploitability: low — yq runs on trusted in-repo YAML/JSON during vessel setup; no attacker-controlled input path reaches yq in the runtime context.
# Expires: 2026-11-06
# Reviewer: @mvillmow  PR #746
CVE-2026-56852

# GHSA-f88m-g3jw-g9cj
# Category: npm
# Rationale: sharp@0.34.5 (transitive dep of cline@2.14.0 in achaean-cline vessel) inherits libvips CVEs CVE-2026-33327 etc.; fixed in sharp 0.35.0 but cline@2.14.0 pins the older range and no newer cline release resolves it yet.
# Exploitability: low — sharp is used only for local image processing in the vessel runtime; no attacker-controlled image input path reaches it.
# Expires: 2026-11-06
# Reviewer: @mvillmow  PR #746
GHSA-f88m-g3jw-g9cj

# golang stdlib + golang.org/x/net cluster compiled into the gitleaks binary
# (pre-commit golang hook env) shipped in the achaean-claude vessel image.
# Fixed in Go 1.26.6 / 1.25.13 toolchains; no released gitleaks build vendors
# a newer stdlib yet. Not reachable from our secret-scan usage (no untrusted
# network input). Same cluster already suppressed on Scylla.
# Expires: 2026-10-14 | Reviewer: mvillmow | Category: ci-image-tooling (gitleaks)
CVE-2026-33818
CVE-2026-39821
CVE-2026-46600
CVE-2026-56853
CVE-2026-56858
CVE-2026-56859
CVE-2026-56860
CVE-2026-56862

# CVE-2024-47081
# Category: npm
# Rationale: glob: symlink traversal — bundled node_modules of npm CLI in the node base image (upstream Node.js LTS pin; not pinable from vessel requirements). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2024-47081

# CVE-2025-50181
# Category: npm
# Rationale: brace-expansion ReDoS — bundled node_modules of npm CLI (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: medium — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2025-50181

# CVE-2025-50182
# Category: npm
# Rationale: brace-expansion ReDoS — bundled node_modules of npm CLI (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2025-50182

# CVE-2025-53643
# Category: npm
# Rationale: diff ReDoS — bundled node_modules of npm CLI (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2025-53643

# CVE-2025-5889
# Category: npm
# Rationale: tar: arbitrary file write during extraction — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2025-5889

# CVE-2025-68146
# Category: npm
# Rationale: minimatch ReDoS — bundled node_modules of npm CLI (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: medium — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2025-68146

# CVE-2025-69224
# Category: npm
# Rationale: tar: symlink traversal — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2025-69224

# CVE-2025-69225
# Category: npm
# Rationale: tar: symlink traversal — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2025-69225

# CVE-2025-69226
# Category: npm
# Rationale: tar: symlink traversal — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2025-69226

# CVE-2025-69227
# Category: npm
# Rationale: tar: symlink traversal — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: medium — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2025-69227

# CVE-2025-69228
# Category: npm
# Rationale: tar: symlink traversal — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: medium — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2025-69228

# CVE-2025-69229
# Category: npm
# Rationale: tar: symlink traversal — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: medium — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2025-69229

# CVE-2025-69230
# Category: npm
# Rationale: tar: symlink traversal — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2025-69230

# CVE-2025-8869
# Category: npm
# Rationale: glob: ReDoS — bundled node_modules of npm CLI (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: medium — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2025-8869

# CVE-2026-1703
# Category: npm
# Rationale: tar: denial of service — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-1703

# CVE-2026-22701
# Category: npm
# Rationale: Pygments ReDoS — bundled npm CLI deps (upstream; Pygments python pin covers the aider venv only). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-22701

# CVE-2026-23950
# Category: npm
# Rationale: pip wheel extraction — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-23950

# CVE-2026-24842
# Category: npm
# Rationale: Pygments ReDoS — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-24842

# CVE-2026-29786
# Category: npm
# Rationale: Pygments ReDoS — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-29786

# CVE-2026-31802
# Category: npm
# Rationale: python-dotenv injection — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-31802

# CVE-2026-33671
# Category: npm
# Rationale: Pygments ReDoS — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-33671

# CVE-2026-33672
# Category: npm
# Rationale: Pygments ReDoS — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-33672

# CVE-2026-33750
# Category: npm
# Rationale: Pygments ReDoS — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-33750

# CVE-2026-34513
# Category: npm
# Rationale: aiohttp smuggling family — bundled npm CLI deps (upstream; python pin covers the aider venv only). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-34513

# CVE-2026-34514
# Category: npm
# Rationale: aiohttp smuggling family — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-34514

# CVE-2026-34515
# Category: npm
# Rationale: aiohttp smuggling family — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: medium — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-34515

# CVE-2026-34516
# Category: npm
# Rationale: aiohttp smuggling family — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: high — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-34516

# CVE-2026-34517
# Category: npm
# Rationale: aiohttp smuggling family — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-34517

# CVE-2026-34518
# Category: npm
# Rationale: aiohttp smuggling family — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-34518

# CVE-2026-34519
# Category: npm
# Rationale: aiohttp smuggling family — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-34519

# CVE-2026-34520
# Category: npm
# Rationale: aiohttp smuggling family — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: critical — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-34520

# CVE-2026-34525
# Category: npm
# Rationale: aiohttp smuggling family — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: medium — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-34525

# CVE-2026-4539
# Category: npm
# Rationale: tar: denial of service — bundled npm CLI deps (upstream). Not pinable from the vessel requirements; upstream fix requires a Node base-image bump.
# Exploitability: low — no attacker-controlled input reaches these bundled npm CLI internals in the vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-4539

# GHSA-6v7p-g79w-8964
# Category: python
# Rationale: msgpack 1.1.2 vendored inside pip 26.2.1 (pip/_vendor/msgpack). pip 26.2.1 is the latest release and cannot be overridden without forking pip; the site-packages msgpack is already bumped to 1.2.1.
# Exploitability: none — pip's vendored msgpack is internal to the installer and never reachable from vessel runtime code.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
GHSA-6v7p-g79w-8964

# CVE-2025-47273
# Category: python
# Rationale: setuptools 70.3.0 vendored inside pip 26.2.1 (pip/_vendor/setuptools). pip 26.2.1 is the latest release and cannot be overridden without forking pip; the site-packages setuptools is already bumped to 83.0.0.
# Exploitability: none — pip's vendored setuptools is internal to the installer and never reachable from vessel runtime code.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2025-47273

# CVE-2026-59890
# Category: python
# Rationale: setuptools runtime fork protection CVE — fixed in 83.0.0, which is pinned in bases/Dockerfile.python. Entry retained as documentation while the pinned 83.0.0 propagates through the base-image rebuild cache.
# Exploitability: none — the pinned setuptools 83.0.0 already carries the fix.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-59890

# CVE-2026-23949
# Category: python
# Rationale: jaraco.context 5.3.0 was vendored inside setuptools 78.1.1 (_vendor). The pinned setuptools 83.0.0 no longer vendors jaraco.context, so this entry documents the transient state during the base-image rebuild cache transition.
# Exploitability: none — vendored build-time dependency, not reachable at vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-23949

# CVE-2026-24049
# Category: python
# Rationale: wheel 0.45.1 was vendored inside setuptools 78.1.1 (_vendor). The pinned setuptools 83.0.0 vendors wheel 0.46.3 (fix in 0.46.2), so this entry documents the transient state during the base-image rebuild cache transition.
# Exploitability: none — vendored build-time dependency, not reachable at vessel runtime.
# Expires: 2026-11-16
# Reviewer: @mvillmow  PR #TBD (fleet security sweep)
CVE-2026-24049

# CVE-2026-56864
# Category: other
# Rationale: golang.org/x/mod v0.37.0 is bundled in the current released gh package; the fixed v0.40.0 is not present in an available upstream gh release. Replace this temporary entry when the gh image rebuild in #779 is available and the clean scan confirms the module update.
# Exploitability: low — gh is a developer CLI and this module path is not exposed as a service endpoint in the vessel runtime.
# Expires: 2026-10-12
# Reviewer: @mvillmow  PR #777 / issue #779
CVE-2026-56864

# CVE-2026-56865
# Category: other
# Rationale: golang.org/x/mod v0.37.0 is bundled in the current released gh package; the fixed v0.40.0 is not present in an available upstream gh release. Replace this temporary entry when the gh image rebuild in #779 is available and the clean scan confirms the module update.
# Exploitability: low — gh is a developer CLI and this module path is not exposed as a service endpoint in the vessel runtime.
# Expires: 2026-10-12
# Reviewer: @mvillmow  PR #777 / issue #779
CVE-2026-56865

